package com.jdojo.exerciciosT3;

import javafx.application.Application;
import javafx.scene.control.Button;
import javafx.scene.control.ComboBox;
import javafx.stage.Stage;

public class Exercicio6_XestorModalidade extends Application{
	
	public void start(Stage stage) throws Exception{
		
		
		ComboBox<String> comboModalidad
		comboModality.getItems().add
		Button abrirVenta = new Button();
	});
	
	VBox root = new VBox(10, comboModality);
	Scene scene = new Scene(root);
	
	stage.setScene(scene);
	stage.setTitle("Xestor de modalidade");
	stage.show();

}

public static void main(String[] args) {
	launch(args);
}
